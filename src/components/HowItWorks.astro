<section class="how-it-works">
	<div class="container">
		<div class="section-heading">
			<h2>Um caminho simples para transformar curiosidade em prática.</h2>
		</div>

		<div class="steps">
			<article class="step">
				<span class="step-number">01</span>
				<div>
					<h3>Escolha uma trilha</h3>
					<p>Encontre o tema que combina com o que você quer aprender agora.</p>
				</div>
			</article>

			<span class="connector" aria-hidden="true"></span>

			<article class="step">
				<span class="step-number">02</span>
				<div>
					<h3>Estude o conteúdo</h3>
					<p>Aprenda no seu ritmo com materiais objetivos e fáceis de acompanhar.</p>
				</div>
			</article>

			<span class="connector" aria-hidden="true"></span>

			<article class="step">
				<span class="step-number">03</span>
				<div>
					<h3>Pratique com projetos</h3>
					<p>Coloque o conhecimento em ação e construa algo que seja seu.</p>
				</div>
			</article>
		</div>
	</div>
</section>

<style>
	.how-it-works {
		padding: 54px 0 30px;
	}

	.section-heading {
		max-width: 100%;
		margin-bottom: 28px;
	}

	h2 {
		margin: 0;
		max-width: 52rem;
		background: linear-gradient(100deg, #ffffff 8%, #d6b5ff 52%, #ff75b7 100%);
		background-clip: text;
		-webkit-background-clip: text;
		color: transparent;
		font-size: clamp(1.75rem, 3.2vw, 2.55rem);
		font-weight: 700;
		line-height: 1.15;
		letter-spacing: -0.02em;
	}

	.section-heading::after {
		display: block;
		width: 72px;
		height: 3px;
		margin-top: 18px;
		content: '';
		border-radius: 3px;
		background: linear-gradient(90deg, #ff4fa3, #8b3ce0);
	}

	.steps {
		display: grid;
		grid-template-columns: 1fr auto 1fr auto 1fr;
		align-items: stretch;
		gap: 14px;
	}

	.step {
		display: flex;
		align-items: flex-start;
		gap: 16px;
		min-height: 148px;
		padding: 22px 20px;
		border: 1px solid rgb(192 132 252 / 18%);
		border-radius: 16px;
		background: linear-gradient(145deg, rgb(26 25 67 / 92%), rgb(15 20 50 / 88%));
		box-shadow: inset 0 1px 0 rgb(255 255 255 / 8%);
		transition: border-color 0.25s ease, transform 0.25s ease, box-shadow 0.25s ease;
	}

	.step:hover {
		border-color: rgb(255 79 163 / 58%);
		box-shadow: 0 14px 30px rgb(3 5 25 / 32%), inset 0 1px 0 rgb(255 255 255 / 12%);
		transform: translateY(-5px);
	}

	.step-number {
		display: grid;
		place-items: center;
		width: 42px;
		height: 42px;
		flex: 0 0 42px;
		border: 1px solid rgb(255 255 255 / 25%);
		border-radius: 50%;
		background: linear-gradient(135deg, #ff4fa3, #8b3ce0);
		color: #fff;
		font-size: 0.95rem;
		font-weight: 800;
	}

	h3 {
		margin: 2px 0 8px;
		color: #fff;
		font-size: 1.08rem;
		font-weight: 600;
		line-height: 1.3;
	}

	.step p {
		margin: 0;
		color: #bfc0d7;
		font-size: 0.88rem;
		line-height: 1.55;
	}

	.connector {
		align-self: center;
		width: 28px;
		height: 1px;
		background: linear-gradient(90deg, #ff4fa3, #8b3ce0);
		opacity: 0.7;
	}

	@media (max-width: 900px) {
		.steps {
			grid-template-columns: 1fr;
		}

		.connector {
			width: 1px;
			height: 24px;
			margin: -4px auto;
			background: linear-gradient(180deg, #ff4fa3, #8b3ce0);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.step {
			transition: none;
		}
	}
</style>
